<div class="jumbotron">
<h1>cilantro</h1>
<p>a recipe keeper</p>
   <% if current_user %>
 </div>
    <%= render partial: 'tags/tags', locals: { tags: current_user.tags } %>
    <%= render partial: 'recipes/recipes', locals: { recipes: current_user.recipes } %>
   <% else %>
    <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary' %> or 
    <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary' %>
   <% end %>
</div>